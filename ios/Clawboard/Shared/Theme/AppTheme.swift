import SwiftUI

enum AppTheme {
    static let brand = Color(red: 0.37, green: 0.49, blue: 1.0)
    static let success = Color.green
    static let warning = Color.orange
    static let danger = Color.red
    static let cardBackground = Color(.secondarySystemBackground)
}
